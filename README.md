# CodeBugs - Github Page

Before:

```
	$ gem install bundler
```

Then:

```
	$ git clone https://github.com/affo/codebugs.git
	$ cd codebugs
	$ bundle install
	$ jekyll serve --baseurl ''
```

Navigate to `http://localhost:4000`
